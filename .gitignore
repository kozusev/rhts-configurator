/node_modules
/.next/
/out/
/build
*.pem
.DS_Store
npm-debug.log*
.env
.env*.local
# Local database is NEVER committed — production uses Postgres (DATABASE_URL).
# This is what stops local data from overwriting the live database on deploy.
/prisma/*.db
/prisma/*.db-journal
/prisma/*.db-wal
/prisma/*.db-shm
next-env.d.ts

# Generated offer emails/PDFs (contain customer data) and build cache
/.mail-preview/
tsconfig.tsbuildinfo
